#include <stdio.h>
//int main()
//{
//	int a = 10;//4个字节
//	//&a;//取地址a
////	printf("%p\n", &a);// 000000867DEFF774
//	int* p = &a;//取地址
//	//有一种变量是用来存放地址的--指针变量
////	printf("%p\n", p);
//	//*p//*--解引用操作符  对p进行解引用  找到他所指向的对象
//	*p = 20; //通过*p找到的那个值改为20 
//	printf("%d\n", a);
//	return 0;
//}
int main()
{
	char ch = 'w';
	char* pc = &ch;
	*pc = 'a';
	printf("%p\n", pc);
	printf("%d\n", sizeof(pc));
	printf("%c\n", ch);
	return 0;
}
